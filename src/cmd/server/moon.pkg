import {
  "f4ah6o/http11" @lib,
  "moonbitlang/async",
  "moonbitlang/async/socket" @socket,
}

options(
  "is-main": true,
  targets: {
    "main_native.mbt": [ "native" ],
    "main_stub.mbt": [ "not", "native" ],
  },
)
